import React from 'react'
import ReactDOM from 'react-dom/client'
import LateNightHacker from '../late-night-hacker.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LateNightHacker />
  </React.StrictMode>,
)
